<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.delterium.tecno_utils"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_USB" />

    <application
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault.Settings">

        <activity
            android:name=".SettingsActivity"
            android:label="@string/otg_settings_title"
            android:icon="@drawable/ic_qs_otg_off"
            android:exported="true">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.connect" />
        </activity>

        <service
            android:name=".OtgTileService"
            android:label="@string/otg_settings_title"
            android:icon="@drawable/ic_qs_otg_off"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".OtgService"
            android:exported="false" />

    </application>
</manifest>